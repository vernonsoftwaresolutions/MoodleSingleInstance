--- 
machine: 
  python: 
    version: "2.7.11"
  ruby: 
    version: "2.4.0"
  services: 
    - docker

dependencies: 
  override: 
    - "docker info"
 #   - "docker build --rm=false -t vernonsoftwaresolutions/moodlesingleinstance:$CIRCLE_BUILD_NUM ."

#test: 
#  post: 
  #  - "docker run -d -p 80:80 -e \"VIRTUAL_HOST=localhost\" --name moodle vernonsoftwaresolutions/moodlesingleinstance:$CIRCLE_BUILD_NUM; sleep 10"
 ##   - "docker ps"
  #  - "docker images"
  #  - "docker port moodle"
  #  - "curl -L http://localhost/moodle"
deployment: 
  development: 
    branch: moodle29
    commands: 
      - "docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS"
    #  - "docker push vernonsoftwaresolutions/moodlesingleinstance:$CIRCLE_BUILD_NUM"
      - "aws configure set default.region us-east-1"
      - "aws configure set default.output json"
      - "aws cloudformation package --template-file cloudformation/ecs.yml --output-template-file ecs-output.yml --s3-bucket circleci.deployables"
      - "aws cloudformation deploy --template-file ecs-output.yml --capabilities CAPABILITY_IAM --stack-name moodle-ecs --parameter-overrides Version=$CIRCLE_BUILD_NUM KeyName=dummy_key1 VpcId=vpc-c7aa77be SubnetId=subnet-b61d81fe,subnet-0202dc58 InstanceType=t2.medium DNSName=moodle.vssdevelopment.com HostedZoneName=vssdevelopment.com." 